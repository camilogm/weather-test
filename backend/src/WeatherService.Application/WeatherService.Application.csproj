<Project Sdk="Microsoft.NET.Sdk">

  <!--
    The core: domain model, ports and use cases.
    It knows nothing about HTTP, EF, IMemoryCache or Open-Meteo.
    Logging abstractions are the only thing allowed in.
    If an infrastructure PackageReference ever appears here, something went wrong.
  -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>
